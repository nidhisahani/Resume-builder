import { EDIT_RESUME } from "../Actions/ActionType";
import initialState from './initialState.json';

export const editReducer =(state=initialState,action)=>{
    switch(action.type){
        
    }
}